answer = document.getElementById('answer-' + <%= "#{@answer.id}" %>);
answer.parentNode.removeChild(answer);
$('.best_answer').html('<%= j render @question.best_answer if @question.best_answer %>');
$('.all_answers').html('<%= j render @question.answers_without_best %>');
