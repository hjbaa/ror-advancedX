$('.new-answer-errors').html('<%= render 'shared/errors', object: @answer %>');

<% if @answer.persisted? %>
  $('.answers').append('<%= j render @answer %>');
  $('.new-answer').html('<%= j render 'answers/new-answer-form', question: @question, answer: Answer.new %>')
<% end %>
