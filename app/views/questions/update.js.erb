$('.question .errors').html('<%= render 'shared/errors', object: @question %>');

<% unless @question.errors.present? %>
  $('.question').html('<%= j render 'question-header', question: @question %>');
<% end %>
